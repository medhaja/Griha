<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahe.griha">

    <application
        android:allowBackup="true"
        android:icon="@drawable/dumbbellsracks_freeweights_gallery_03"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Plank" />
        <activity android:name=".pullup" />
        <activity android:name=".plank2" />
        <activity android:name=".pushup" />
        <activity android:name=".pullup2" />
        <activity android:name=".pushup2" />
    </application>

</manifest>